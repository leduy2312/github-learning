[khoi tao local repository]
git init .

git add .

git commit -m "your message"

git log

git remote add origin < remote repository url>

git push -u origin master

###truong hop so 2 day git code moi len>>
1.git add -A
2.git commit -m "<commit message>"
3.git push 




{ADDITIONAL GIT CLI}
git branch